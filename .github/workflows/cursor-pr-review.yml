name: Cursor PR Review (Retired)

# Retired per D-060 / D-056: Bugbot + BUGBOT.md owns the Pillar B review loop.
on:
  workflow_dispatch:

jobs:
  retired:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Retired workflow
        run: echo "Use Bugbot Autofix — see BUGBOT.md and docs/CURSOR-AGENT-HANDBOOK.md"
