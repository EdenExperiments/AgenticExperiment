---
description: Entry points, zones, skills, and guides — single routing rule
alwaysApply: true
---

# Repo Routing

**Entry points:** `AGENTS.md` · `docs/README.md` · `docs/CURSOR-AGENT-HANDBOOK.md` (CI/ops only).

**Zones:** `apps/api/` (Go) · `apps/rpg-tracker/`, `apps/nutri-log/`, `apps/mental-health/` (frontends) · `packages/*` (shared).

**Skills:** pstack (`/poteto-mode`) and cursor-team-kit. Models: `.cursor/rules/pstack-models.mdc`. LifeQuest browser: `.cursor/skills/verify-lifequest/`.

**Durable docs:** `docs/`. **Ephemeral plans:** `docs/briefs/`. Do not write `Documentation/delivery/`.
